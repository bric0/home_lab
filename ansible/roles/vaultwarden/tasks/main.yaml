---
- name: Setting up Vaultwarden
  block:
    - name: Create Vaultwarden data directory
      ansible.builtin.file:
        path: "{{ vaultwarden_data_dir }}/data"
        state: directory
        owner: root
        group: root
        mode: '0755'
  become: true
