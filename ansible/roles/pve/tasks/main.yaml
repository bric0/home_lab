---
- name: Setting up network interfaces
  block:
    - name: Deploy interfaces configuration
      ansible.builtin.template:
        src: interfaces.j2
        dest: /etc/network/interfaces
        owner: root
        group: root
        mode: '0644'
        backup: true
      notify: Reload network
  become: true
